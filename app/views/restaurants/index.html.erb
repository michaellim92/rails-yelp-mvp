<h1>All restaurants</h1>
<%= link_to 'Add restaurant', new_restaurant_path(@restaurant)%>
<% @restaurants.each do |restaurant| %>
  <h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3><br />
  <p><%= restaurant.category %></p>
  <p><%= '⭐️' %></p>
<% end %>
